<% if tasks %>
  <ul>
    <% tasks.each do |task| %>
      <% title = task.dig('properties', 'Name', 'title', 0, 'plain_text') %>
      <% next if title.nil? %>
      <% title.strip! %>
      <% url = "#{NOTION_BASE_URL}#{title.tr(" ", "-")}-#{task['id'].tr("-", "")}" %>
      <li style="list-style: none">
        <input type="checkbox" onClick="completeTask('<%= task['id'].tr("-", "") %>')">
        <a href="<%= url %>" target="_blank"><%= title %></a>
        <span style="color: #ccc; font-size: 0.9rem">(<%= task.dig('properties', 'Status', 'select', 'name') %>)</span>
      </li>
    <% end %>
  </ul>
<% end %>
