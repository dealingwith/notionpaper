<%= erb '_head'.to_sym %>

<p>Message: <%= show_message %></p>

<p>DB ID: <%= session[:db_id] %></p>
<p>filter_property: <%= session[:filter_property] %></p>
<p>filter_type: <%= session[:filter_type] %></p>
<p>filter_options: <%= session[:filter_options] %></p>

<div class="tasks">
  <%= erb '_tasks'.to_sym, :locals => {:tasks => tasks, filter_options: filter_options } %>
</div>

<p><a href="/config_database">Configure</a></p>
