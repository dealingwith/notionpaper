<p>Choose filters:</p>
<ul>
  <% filter_options_data.each do |filter_option| %>
    <li>
      <label><input type="checkbox" name="<%= filter_option['name'] %>" /> <%= filter_option['name'] %></label>
    </li>
  <% end %>
  <a href="" id="multi-filter">Use multiple filters</a>
</ul>
<button id="multi-filter">Submit</a>
<script>
  document.getElementById('multi-filter').addEventListener('click', function(e) {
    e.preventDefault();
    var checkboxes = document.querySelectorAll('input[type="checkbox"]');
    var checked = [];
    for (var i = 0; i < checkboxes.length; i++) {
      if (checkboxes[i].checked) {
        checked.push(checkboxes[i].name);
      }
    }
    window.location.href = '/?filter_options=' + checked.join(',');
  });
</script>
